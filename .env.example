# Database file location (relative path is fine for local dev)
DATABASE_URL=file:./db/custom.db

# Optional: local backups directory (default: ./backups)
# BACKUP_DIR=./backups

# Optional: timezone (default: Asia/Tehran)
# TZ=Asia/Tehran
