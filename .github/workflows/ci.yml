name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bunx prisma generate

      - name: Custom-path backup test
        run: bun run test:custom-paths

      - name: Panel-client regression test (#6 retry, #8 cleanup, PG users.json)
        run: bun run test:panel-fixes

      - name: Shell script syntax check
        run: |
          FAIL=0
          while IFS= read -r -d '' f; do
            if ! bash -n "$f"; then
              echo "::error::syntax error in $f"
              FAIL=1
            fi
          done < <(git ls-files -z '*.sh')
          exit $FAIL

      - name: Type check
        run: bunx tsc --noEmit

      - name: Lint
        run: bun run lint

      - name: Build
        run: bun run build
        env:
          DATABASE_URL: file:./db/custom.db

  version-guard:
    # A release tag must ALWAYS match the version in package.json — the
    # installer and updater refuse to install a release whose code version
    # doesn't match its tag.
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Tag ↔ package.json version check
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          PKG_V="v$(node -p "require('./package.json').version")"
          if [ "$TAG" != "$PKG_V" ]; then
            echo "::error::tag $TAG does not match package.json version $PKG_V"
            exit 1
          fi
          echo "OK: tag $TAG matches package.json $PKG_V"
